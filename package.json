{
    "name": "ui-interaction",
    "version": "0.1.0",
    "private": true,
    "dependencies": {
        "axios": "^0.19.1",
        "json-server": "^0.16.2",
        "react": "^16.12.0",
        "react-dom": "^16.12.0",
        "react-iframe": "^1.8.0",
        "react-router-dom": "^5.1.2",
        "react-scripts": "3.4.0",
        "styled-components": "^4.4.1",
        "dotenv": "^8.2.0",
        "cross-env": "^7.0.2"
    },
    "devDependencies": {
        "gh-pages": "^2.2.0",
        "react-iframe": "^1.8.0"
    },
    "scripts": {
        "start": "react-scripts start",
        "build": "react-scripts build",
        "test": "react-scripts test",
        "eject": "react-scripts eject",
        "predeploy": "yarn build",
        "deploy": "gh-pages -d build",
        "json-server": "json-server --watch db.json --port 3001",
        "forever": "forever start -w --watchDirectory ./build/animation node.js"
    },
    "eslintConfig": {
        "extends": "react-app"
    },
    "browserslist": {
        "production": [
            ">0.2%",
            "not dead",
            "not op_mini all"
        ],
        "development": [
            "last 1 chrome version",
            "last 1 firefox version",
            "last 1 safari version"
        ]
    }
}
